import Hero from "@/components/hero/hero";

const Home = async () => {
  // createAudio("hello world");
  // createVideo()
  return (
    <main className="max-w-7xl mx-auto">
      <Hero />
    </main>
  );
};

export default Home;
